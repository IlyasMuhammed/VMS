// Used by `ng serve` / development builds. Swapped for environment.prod.ts by `ng build`
// (see angular.json's production fileReplacements).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api',
};
